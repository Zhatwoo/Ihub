// Room validation schemas
// TODO: Implement validation using Joi or similar

export const createRoomSchema = {
  // TODO: Define validation schema for creating room
  // Example:
  // name: Joi.string().required(),
  // type: Joi.string().valid('private-office', 'meeting-room', 'conference-room').required(),
  // capacity: Joi.number().integer().min(1).required(),
  // floor: Joi.string().required()
};

export const updateRoomSchema = {
  // TODO: Define validation schema for updating room
};

export const roomQuerySchema = {
  // TODO: Define validation schema for room query parameters
};
