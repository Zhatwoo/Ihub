// Schedule validation schemas
// TODO: Implement validation using Joi or similar

export const createScheduleSchema = {
  // TODO: Define validation schema for creating schedule
  // Example:
  // roomId: Joi.string().required(),
  // userId: Joi.string().required(),
  // email: Joi.string().email().required(),
  // startTime: Joi.date().required(),
  // endTime: Joi.date().required().greater(Joi.ref('startTime')),
  // date: Joi.date().required()
};

export const updateScheduleSchema = {
  // TODO: Define validation schema for updating schedule
};

export const scheduleQuerySchema = {
  // TODO: Define validation schema for schedule query parameters
};
