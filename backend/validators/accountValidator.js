// Account validation schemas
// TODO: Implement validation using Joi or similar

export const createAccountSchema = {
  // TODO: Define validation schema for creating account
  // Example using Joi:
  // import Joi from 'joi';
  // export const createAccountSchema = Joi.object({
  //   email: Joi.string().email().required(),
  //   password: Joi.string().min(6).required(),
  //   type: Joi.string().valid('client', 'admin').required(),
  //   name: Joi.string().required()
  // });
};

export const updateAccountSchema = {
  // TODO: Define validation schema for updating account
};

export const accountQuerySchema = {
  // TODO: Define validation schema for account query parameters
};
